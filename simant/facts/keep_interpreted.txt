# SIMANTW keep-interpreted recovery facts (DOS_RE 2.0 dos_re_2.0.md §3:
# game-specific recovery fact, evidence-backed).  One NE_SEG:HEX_OFFSET per
# line (NE segment index is decimal 1-7); consumed by scripts/irgen.py and
# tagged env_wait in the recovery IR (the keep-interpreted queue mechanism —
# these functions stay OUTSIDE the VMless graph as the enumerated, fail-loud
# frontier until a capability dissolves them).
#
# History: the cont.219 census seeded this file with the full scan frontier —
# 33 SYM entries over 32 addresses (32x inline x87 + _DoInt3).  The x87 class
# (31 addresses: the MSC FP runtime in _TEXT + _AdjustWndMinMax in seg 1)
# DISSOLVED when dos_re gained native x87 ESC scanning/emission via the
# interpreter's own FPU helpers (dos_re 24b4366; journal cont.224) — those
# entries lift natively now and left this file, exactly as the charter above
# prescribes.
#
# What remains is the one genuine dead-code stub:
#   * _DoInt3 (7:F85B) — a debug-break stub whose body decodes straight into
#     0xFFFF segment padding (grp5 invalid /r): not liftable because it is
#     not code.  Evidence it is DEAD: zero static callers in the whole
#     recovery IR (no calls_near/calls_far edge targets 7:F85B), consistent
#     with an int3 hook stubbed out for release.  It stays here (rather than
#     in new exclusion machinery) because keep-interpreted already expresses
#     precisely the right contract: outside the graph, fail-loud if anything
#     ever reaches it.
7:F85B  # SIMTWO_MODULE!_DoInt3 -- grp5 invalid /r (0xFFFF padding body); dead code: 0 static callers in the IR
