"""The layout bridge + state-view seam for the VM-less port (see
``dgroup_view.py``).  Pure Python — never imports the VM (dos_re / win16) or the
game's runtime; importable by both recovered logic and the VM island adapters.
"""
